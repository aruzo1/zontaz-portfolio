import { HeroCarousel } from "./hero-carousel";
import { HeroText } from "./hero-text";

export const HeroSection = () => {
  return (
    <main>
      <HeroText />
      <HeroCarousel />
    </main>
  );
};
